# 통합 갱신·종합추천·성과검증

구현 기준: 2026-09-07. 아래는 구축·검증 시점의 기록입니다. 2026-09-07 사용자의 배포 요청에 따라 검증된 9월 4일 기준 결과와 새 기능을 게시하며, 실제 공개 확인시각과 성과 추적 시작은 추천 원장에 기록합니다.

검증: Python 회귀 175개 및 화면 스크립트 실행 검사 14개 통과. 2,765종목 저장자료 전수 재계산 2회와 외부 통신 0회 확인. 실제 캐시 아카이브(5,732파일)를 임시 위치로 복원한 뒤 가격 2,765종목·재무 2,555종목·스냅샷 무결성을 확인했습니다. 브라우저 제어 연결이 없어 실제 배치·색상·반응형 시각 검사는 수행하지 못했습니다.

## 무엇이 달라졌는지

- 가격·재무를 공통 검증 스냅샷으로 저장하고 모든 평가창에서 같은 가격 기준일을 사용합니다.
- 매번 모든 API를 재호출하지 않습니다. 새 거래일, 변경 공시 접수번호, 재확인 기간이 된 종목을 우선 수집합니다.
- 가격의 실제 날짜, 원주가/수정주가, 실제 거래대금/종가×거래량 근사를 구별합니다. 기존 출처 미상 값을 소급 검증한 것으로 만들지 않습니다.
- DART는 최신 보고분기에 맞춰 실제 4분기를 재구성합니다. 연결·별도, 보고기간, 접수번호가 맞지 않으면 억지로 합산하지 않습니다.
- 컨센서스의 월 단위 날짜나 미상 날짜를 특정 일자로 만들지 않습니다. 추정 연도가 같은 값끼리만 변경률을 비교합니다.
- 공시 원문 파싱, 뉴스 검색 큐, 무역통계 확인을 캐시합니다. 뉴스 제목·IR 개최 일정만으로 성장 사실을 만들지 않습니다.
- 실패한 평가창은 원래 자료와 날짜를 유지하며 다른 창의 계산을 막지 않습니다. 기존 보유수량·평단·주관적 판단을 보존합니다.
- 기존 표 제목·열 순서·표시 방식은 `ui_contract.json` 그대로입니다. 가치 점수에는 T+를 넣지 않았습니다.

## 종합추천 규칙: combined-1.0

화면 상위 15·20개끼리 교집합을 구하지 않습니다. 전체 계산 후보에서 같은 가격 기준일의 조건을 조합합니다.

| 구분 | 초기 규칙 |
|---|---|
| 진입 | 기존 진입가능 또는 곧진입 조건. 곧진입은 관찰 후보이며 실행 신호로 취급하지 않음 |
| 가치 | 정상화 P/OP 1~10배, 섹터 중앙보다 저렴함, 실제 4분기, 신뢰도 A/B |
| 성장 | 독립 근거 2개 이상, 기존 성장점수 30 이상, 미반영 가능 또는 일부 가격 반응 |
| 조합 | 진입+가치, 진입+성장, 진입+가치+성장 |
| 정렬 | 진입가능 우선, 통과 조건 점수의 단순 평균 내림차순, 동점은 종목코드 |

진입점수만 0~100으로 제한하여 평균을 구합니다. 기존 점수를 바꾸거나 다른 평가창에 역으로 전달하지 않습니다. 점수 분포가 서로 다르므로 이 조합은 **성과가 입증되지 않은 초기 실험 규칙**입니다. 상위 10개는 매수 지시가 아닙니다. 조건별 성과를 쌓기 전에 유리한 과거 결과에 맞춰 가중치를 최적화하지 않습니다.

현재 저장자료 통합시험: 가격 2026-09-04, 2,765종목 / 1,006,725개 일별 가격행 / 재무 2,555종목. 전체 계산 후보는 진입 495개, 가치 1,588개, 성장 388개, 순환 1,636개. 종합 조건 충족 158개 중 10개 표시. 외부 네트워크 호출 0회. 이 결과는 **9월 7일 현재가격 전수 갱신 결과가 아닙니다.**

## 성과검증 원칙

1. 산출시각과 실제 사이트 공개를 확인한 시각을 별도로 저장합니다. 사이트에 동일 회차와 동일 내용이 올라온 것을 확인하기 전에는 추천 원장에 기록하지 않습니다.
2. 추천 당시 순위·조건·수치·엔진 버전·규칙 버전·시장 구성종목을 보존합니다. 공개 재확인 재시도는 중복 기록하지 않습니다.
3. 원장은 모든 공개회차를 보존하되, 통계는 평가창·엔진 버전·규칙 버전·추천일별 최종 공개 확인본만 사용합니다.
4. 기준가격은 공개 확인일 **다음 실제 거래일 시가**입니다. 당일 이미 지나간 시가·전일 종가를 매수가로 쓰지 않습니다. 진입일 자체를 1거래일로 계산합니다.
5. 5·20·60·120거래일 수익률, 현재까지 수익률, 현재까지 종가 기준 최대낙폭, 조건별 평균·중앙값·상승 비율을 표시합니다.
6. 가격 누락 시 나중의 거래일로 진입일을 옮기지 않습니다. 거래정지·상장폐지 미확인, 수정주가 미확인, 거래일 달력 미확인은 별도 대기/누락입니다. 미도래나 누락을 0%로 넣지 않습니다.
7. 검증된 공급자 수정주가와 같은 조정계수를 적용한 시가를 사용합니다. 배당 처리는 공급자의 조정 방식에 따르므로 이를 무조건 총수익률로 부르지 않습니다. 실제 거래의 실현손익이 아닙니다.
   기본 수집원이 원주가만 제공하면 공개 확인된 추천 종목만 Yahoo 수정주가로 보완합니다. 전체 시장을 이 목적으로 다시 수집하지 않으며, 보완 실패 종목은 기존 수정주가와 원래 날짜를 보존하고 누락 상태로 남깁니다. 이 보완 가격은 기존 가치 엔진 입력을 바꾸지 않습니다.
8. 시장 비교는 추천 당시 전체시장 동일비중입니다. 해당 구성종목의 시작·종료 가격이 모두 확보되어야만 표시합니다. 누락 종목을 빼고 생존 종목만으로 비교하지 않습니다.
9. 비용은 왕복 합산 bp이며 기본 0입니다. 실제 수수료·세금·슬리피지를 반영하려면 명시적으로 설정합니다. 시장 비교에도 같은 비용을 적용합니다.
10. 동일 종목의 반복 추천은 독립 표본이 아닙니다. 고유 종목 수와 평가완료/전체 수를 같이 보며, 포트폴리오 누적수익이나 통계적 유의성을 주장하지 않습니다.

과거 원자료와 공개시각을 정확히 재구성하지 못한 이전 추천은 소급 성과로 넣지 않았습니다. 첫 실제 공개 확인 뒤부터 기록을 쌓으므로 처음에는 성과가 비어 있는 것이 정상입니다.

## 실행과 배포 경계

기본 증분 갱신(게시 파일은 바꾸지 않음):

```text
python refresh_all.py --config config.kis.example.json
```

저장된 원자료만 전체 재계산(두 옵션을 모두 사용하면 외부 수집 없음):

```text
python refresh_all.py --reuse-snapshot --reuse-evidence
python -m unittest discover -s tests -v
python board_contract.py --board test_output/data.test.json
python preview_server.py
```

로컬 미리보기: http://127.0.0.1:8765/ — 후보 자료만 제공하고 캐시·자격증명·원문 파일을 웹에 노출하지 않습니다.

`--full-refresh`는 강제 재확인용입니다. 일상 갱신은 증분 방식을 사용합니다. 최초 실행에는 기간별 공시 캐시와 정확한 대상종목 범위를 한 번 구축해야 하므로 이후 실행보다 요청 수가 많습니다.

**배포를 명시적으로 요청받은 경우에만** 기존 GitHub/Cloudflare Pages 경로로 반영합니다. 수동 워크플로는 검증, 선택 구역 반영, 첫 배포, 사이트 반영 확인, 원장/성과 저장 순서로 동작합니다. 사이트 반영 확인에 실패하면 공개일을 추정해 만들지 않고 실패로 남깁니다. 새 코드 푸시만으로 비싼 전체 수집을 자동 시작하지 않습니다.

`promote_sections.py --research`는 원본 진입·순환·가치·성장 구역과 같이 사용합니다. `--allow-partial`은 실패한 구역의 이전 게시값을 보존합니다. `record_publication.py`는 실제 공개한 회차를 확인하는 별도 단계이며 로컬 미리보기만으로 실행하지 않습니다.

## 저장·복구

- `cache/snapshots/objects/`: 내용 해시별 불변 원자료. 같은 내용은 중복 복사하지 않습니다.
- `cache/snapshots/manifests/`: 원자료 해시·크기·원래 기준일.
- `test_output/verified_snapshot.json`: 검증된 가격·재무·보고서 한 세트를 가리키는 원자적 포인터. 저장 중단 시 이전 세트가 유지됩니다.
- `test_output/full_candidates.json`, `growth_audit.test.json`, `combined_audit.test.json`: 화면에서 자르기 전 전체 후보와 조건 판정.
- `recommendation-history.json`: 공개 확인 추천 원장. Git 이력에 보존합니다.
- 원자료 캐시는 GitHub 실행 간 아카이브로 이어받습니다. 토큰·쿠키·환경 파일·제공받은 자막 원문은 아카이브 대상이 아닙니다.

GitHub 원자료 아카이브에는 보존기간과 저장용량 한계가 있습니다. 90일 보존은 영구 데이터베이스가 아닙니다. 유효한 아카이브가 없으면 조용히 초기화하지 않고 명시적인 `rebuild_cache` 복구 실행을 요구합니다. 장기 원자료 보존이 필요하면 별도의 승인된 저장소·백업 정책을 추가해야 합니다. 추천 기록 자체는 Git에 남습니다.

## 아직 확보되지 않은 입력

- 뉴스 검색: NAVER 자격증명이 없으면 검색도 대기합니다. 검색 성공은 원문 검증 성공이 아닙니다.
- 뉴스·IR 원문: 제공/허용된 원문과 검토된 사실을 `growth_documents.py`의 입력 규격으로 받습니다. 자동 원문 구매·유료벽 우회·임의 미래 숫자 생성은 없습니다.
- 유튜브: 허용되거나 사용자가 제공한 검증 자막만 받습니다. 가격을 갱신했다고 신규 영상 발언을 갱신한 것으로 표시하지 않습니다.
- 이번 작업에서는 외부 API의 실제 신규 전수 수집과 원격 배포를 실행하지 않았습니다. 기존 전시장 자료 재계산과 오프라인 회귀검증으로 구현을 확인했습니다.
